<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>ATS Resume Scanner - Alumni Connect</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
</head>
<body class="bg-slate-100">
    <main class="container mx-auto px-6 py-8">
        <h1 class="text-3xl font-bold text-slate-800 mb-6">AI Resume Scanner</h1>
        <div class="grid md:grid-cols-2 gap-8">
            <div class="bg-white p-8 rounded-lg shadow-md">
                <h2 class="text-2xl font-semibold text-slate-700 mb-4">Analyze Your Resume</h2>
                <form action="/api/scan-resume" method="POST" enctype="multipart/form-data">
                    <div class="mb-4">
                        <label for="resume" class="block text-slate-600 mb-2">Upload Resume (PDF only)</label>
                        <input type="file" name="resume" id="resume" required class="w-full text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-sky-50 file:text-sky-700 hover:file:bg-sky-100"/>
                    </div>
                    <div class="mb-6">
                        <label for="jobDescription" class="block text-slate-600 mb-2">Paste Job Description</label>
                        <textarea name="jobDescription" id="jobDescription" rows="10" required class="w-full border rounded-lg p-3 text-slate-700 focus:ring-2 focus:ring-sky-500 outline-none" placeholder="Paste the full job description here..."></textarea>
                    </div>
                    <button type="submit" class="bg-sky-500 text-white font-bold py-3 px-6 rounded-lg hover:bg-sky-600 w-full">
                        <i class="fas fa-search mr-2"></i>Analyze and Get ATS Score
                    </button>
                </form>
            </div>
            <div class="bg-white p-8 rounded-lg shadow-md">
                <h2 class="text-2xl font-semibold text-slate-700 mb-4">Analysis Result</h2>
                <% if (result) { %>
                    <div class="space-y-4">
                        <div class="text-center">
                            <h3 class="text-lg font-medium text-slate-500">Your ATS Score</h3>
                            <p class="text-6xl font-bold text-sky-600"><%= result.score %>%</p>
                        </div>
                        <div>
                            <h4 class="font-semibold text-slate-800">Strengths:</h4>
                            <p class="text-slate-600"><%= result.strengths %></p>
                        </div>
                        <div>
                            <h4 class="font-semibold text-slate-800">Areas for Improvement:</h4>
                            <p class="text-slate-600"><%= result.improvements %></p>
                        </div>
                    </div>
                <% } else { %>
                    <div class="text-center text-slate-500">
                        <i class="fas fa-file-alt fa-3x mb-4"></i>
                        <p>Your analysis and ATS score will appear here after you submit your documents.</p>
                    </div>
                <% } %>
            </div>
        </div>
    </main>
</body>
</html>