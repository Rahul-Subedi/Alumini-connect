<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Upcoming Events - Alumni Connect</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        body {
            font-family: 'Poppins', sans-serif;
            background-color: #f1f5f9;
        }
        .event-card {
            transition: transform 0.3s ease, box-shadow 0.3s ease;
        }
        .event-card:hover {
            transform: translateY(-5px);
            box-shadow: 0 10px 20px rgba(0,0,0,0.1);
        }
        @keyframes fadeIn {
            from { opacity: 0; transform: translateY(10px); }
            to { opacity: 1; transform: translateY(0); }
        }
        .fade-in {
            animation: fadeIn 0.5s ease-out forwards;
        }
    </style>
</head>
<body class="antialiased">

    <%- include('partials/navbar') %>

    <div class="container mx-auto py-12 px-4">
        <header class="text-center mb-12">
            <h1 class="text-4xl md:text-5xl font-bold text-slate-800">Upcoming Events</h1>
            <p class="mt-2 text-lg text-slate-600">Stay connected with the community through our curated events.</p>
        </header>
        
        <% if (user && user.role === 'admin') { %>
            <div class="flex justify-center mb-10">
                <a href="/events/create" class="bg-sky-600 hover:bg-sky-700 text-white font-bold py-3 px-8 rounded-full transition duration-300 shadow-lg hover:shadow-xl transform hover:scale-105">
                    <i class="fas fa-plus-circle mr-2"></i> Create New Event
                </a>
            </div>
        <% } %>

        <div class="max-w-4xl mx-auto space-y-6">
            <% if (events.length > 0) { %>
                <% events.forEach((event, index) => { %>
                    <div class="event-card bg-white p-6 rounded-xl shadow-md border border-gray-200 flex items-start gap-6 fade-in" style="animation-delay: <%= index * 100 %>ms;">
                        <div class="flex-shrink-0 w-20 h-20 bg-sky-100 rounded-lg flex flex-col items-center justify-center">
                            <span class="text-sky-600 font-bold text-3xl"><%= new Date(event.date).getDate() %></span>
                            <span class="text-sky-500 text-sm font-medium"><%= new Date(event.date).toLocaleDateString('en-US', { month: 'short' }) %></span>
                        </div>
                        <div class="flex-grow">
                            <h2 class="text-2xl font-semibold text-slate-800"><%= event.title %></h2>
                            <p class="text-slate-500 text-sm mt-1">
                                <i class="fas fa-calendar-alt mr-2"></i><%= new Date(event.date).toLocaleDateString('en-US', { weekday: 'long', year: 'numeric', month: 'long', day: 'numeric' }) %>
                            </p>
                            <p class="text-slate-500 text-sm mt-1">
                                <i class="fas fa-map-marker-alt mr-2"></i><%= event.location %>
                            </p>
                            <p class="text-slate-700 mt-3"><%= event.description %></p>
                        </div>
                    </div>
                <% }) %>
            <% } else { %>
                <div class="text-center py-16 bg-white rounded-lg shadow-md border">
                    <i class="fas fa-calendar-times fa-4x text-slate-400 mb-4"></i>
                    <h3 class="text-2xl font-semibold text-slate-700">No Upcoming Events</h3>
                    <p class="text-lg text-slate-500 mt-2">Check back soon for new events and meetups!</p>
                </div>
            <% } %>
        </div>
    </div>
</body>
</html>