<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Admin Dashboard</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@400;500;600;700&display=swap" rel="stylesheet">
    <style> body { font-family: 'Poppins', sans-serif; } </style>
</head>
<body class="bg-gray-100">

    <nav class="bg-gray-800 text-white p-4">
        <div class="container mx-auto flex justify-between items-center">
            <h1 class="text-xl font-bold">Admin Dashboard</h1>
            <div>
                <a href="/" class="hover:text-sky-300 mr-4">View Main Site</a>
                <a href="/logout" class="bg-red-600 hover:bg-red-700 font-semibold py-2 px-4 rounded-lg">Logout</a>
            </div>
        </div>
    </nav>

    <div class="container mx-auto p-8">
        
        <div class="bg-white p-6 rounded-lg shadow-md mb-8">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Pending Alumni Approvals</h2>
            <% if (pendingUsers.length > 0) { %>
                <div class="overflow-x-auto">
                    <table class="min-w-full bg-white">
                        <thead class="bg-gray-200">
                            <tr>
                                <th class="py-2 px-4 text-left">Name</th>
                                <th class="py-2 px-4 text-left">Email</th>
                                <th class="py-2 px-4 text-left">Institute</th>
                                <th class="py-2 px-4 text-center">Document</th>
                                <th class="py-2 px-4 text-center">Actions</th>
                            </tr>
                        </thead>
                        <tbody>
                            <% pendingUsers.forEach(pUser => { %>
                                <tr class="border-b">
                                    <td class="py-2 px-4"><%= pUser.name %></td>
                                    <td class="py-2 px-4"><%= pUser.email %></td>
                                    <td class="py-2 px-4"><%= pUser.institute %></td>
                                    <td class="py-2 px-4 text-center">
                                        <a href="<%= pUser.verification.documentUrl %>" target="_blank" class="text-blue-600 hover:underline">View</a>
                                    </td>
                                    <td class="py-2 px-4 text-center">
                                        <div class="flex justify-center space-x-2">
                                            <form action="/admin/users/approve/<%= pUser._id %>" method="POST" class="inline">
                                                <button type="submit" class="bg-green-500 hover:bg-green-600 text-white text-sm font-semibold py-1 px-3 rounded-md">Approve</button>
                                            </form>
                                            <form action="/admin/users/reject/<%= pUser._id %>" method="POST" class="inline">
                                                <button type="submit" class="bg-red-500 hover:bg-red-600 text-white text-sm font-semibold py-1 px-3 rounded-md">Reject</button>
                                            </form>
                                        </div>
                                    </td>
                                </tr>
                            <% }) %>
                        </tbody>
                    </table>
                </div>
            <% } else { %>
                <p class="text-gray-600">There are no pending approvals at the moment.</p>
            <% } %>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md">
            <h2 class="text-2xl font-bold text-gray-800 mb-4">Event Management</h2>
            <p class="text-gray-600 mb-4">Create and manage events for the alumni network.</p>
            <a href="/events/create" class="bg-blue-600 hover:bg-blue-700 text-white font-bold py-2 px-6 rounded-lg inline-block">
                + Create New Event
            </a>
        </div>

        <div class="bg-white p-6 rounded-lg shadow-md mt-8">
    <h2 class="text-2xl font-bold text-gray-800 mb-4">Fundraising Management</h2>
    <p class="text-gray-600 mb-4">Create and manage fundraising campaigns for the network.</p>
    <a href="/admin/campaigns/new" class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-6 rounded-lg inline-block">
        + Create New Campaign
    </a>
</div>

    </div>
</body>
</html>