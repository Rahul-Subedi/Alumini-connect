<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= profileUser.name %>'s Profile - Alumni Connect</title>
    <script src="https://cdn.tailwindcss.com"></script>
    <link rel="stylesheet" href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.1.1/css/all.min.css">
    <link href="https://fonts.googleapis.com/css2?family=Poppins:wght@300;400;500;600;700&display=swap" rel="stylesheet">
    <style>
        html { background-color: #f1f5f9; }
        body { font-family: 'Poppins', sans-serif; background-color: #f1f5f9; }
        .form-container {
            max-height: 0;
            overflow: hidden;
            transition: max-height 0.7s ease-in-out, padding 0.7s ease-in-out, margin 0.7s ease-in-out;
            padding-top: 0;
            padding-bottom: 0;
            margin-top: 0;
        }
        .form-container.open {
            max-height: 1500px; /* Increased height to accommodate new form */
            padding-top: 2rem;
            padding-bottom: 2rem;
            margin-top: -1px;
        }
    </style>
</head>
<body class="antialiased">
    <%- include('partials/navbar') %>

    <div class="container mx-auto p-4 sm:p-8">
        <div class="bg-white rounded-2xl shadow-xl border border-gray-200 overflow-hidden">
            <div class="p-8 bg-slate-50 border-b">
                <div class="flex flex-col md:flex-row items-center md:items-start text-center md:text-left gap-8">
                    <div class="relative flex-shrink-0">
                        <img class="w-36 h-36 rounded-full object-cover border-4 border-sky-500 shadow-lg" 
                             src="<%= profileUser.profilePictureUrl || `https://ui-avatars.com/api/?name=${encodeURIComponent(profileUser.name)}&background=3b82f6&color=fff&size=144` %>" 
                             alt="<%= profileUser.name %>">
                        <% if(user && user._id.toString() === profileUser._id.toString()) { %>
                            <button onclick="togglePictureForm()" class="absolute -bottom-2 -right-2 bg-white rounded-full p-3 shadow-lg border hover:bg-gray-100 transition-transform transform hover:scale-110">
                                <i class="fas fa-camera text-gray-600"></i>
                            </button>
                        <% } %>
                    </div>
                    <div class="flex-grow mt-4 md:mt-0">
                        <h1 class="text-4xl font-bold text-slate-800"><%= profileUser.name %></h1>
                        <p class="text-xl text-slate-600 mt-1"><%= profileUser.job_title || 'Role not specified' %> at <%= profileUser.current_company || 'Company not specified' %></p>
                        <p class="text-lg text-sky-600 font-medium mt-2"><%= profileUser.institute %>, Batch of <%= profileUser.batch %></p>
                    </div>
                    <% if(user && user._id.toString() === profileUser._id.toString()) { %>
                        <button onclick="toggleEditForm()" class="bg-slate-200 hover:bg-slate-300 text-slate-700 font-semibold py-2 px-4 rounded-lg text-sm flex items-center transition-all">
                            <i class="fas fa-pencil-alt mr-2"></i> Edit Profile
                        </button>
                    <% } %>
                </div>
            </div>

            <div id="pictureForm" class="form-container px-8 border-b">
                <form action="/profile/picture" method="POST" enctype="multipart/form-data" class="p-4 bg-gray-50 rounded-lg flex items-center justify-center gap-4">
                    <input type="file" name="profilePicture" required class="text-sm text-slate-500 file:mr-4 file:py-2 file:px-4 file:rounded-full file:border-0 file:text-sm file:font-semibold file:bg-sky-50 file:text-sky-700 hover:file:bg-sky-100"/>
                    <button type="submit" class="bg-sky-600 hover:bg-sky-700 text-white text-sm font-semibold py-2 px-4 rounded-full">Upload New Picture</button>
                </form>
            </div>

            <div id="editForm" class="form-container px-8 border-b">
                <form action="/profile/update" method="POST" class="space-y-6">
                    <h3 class="text-xl font-semibold text-slate-700 border-b pb-2">Edit Your Details</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div><label class="block text-sm font-medium text-gray-700">Full Name</label><input type="text" name="name" value="<%= profileUser.name %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">Location</label><input type="text" name="location" value="<%= profileUser.location || '' %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">Current Job Title</label><input type="text" name="job_title" value="<%= profileUser.job_title || '' %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">Current Company</label><input type="text" name="current_company" value="<%= profileUser.current_company || '' %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">LinkedIn Profile URL</label><input type="url" name="linkedin_url" value="<%= profileUser.linkedin_url || '' %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">Contact Number</label><input type="text" name="contact" value="<%= profileUser.contact || '' %>" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                    </div>
                    <div><label class="block text-sm font-medium text-gray-700">Professional Bio</label><textarea name="bio" rows="4" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"><%= profileUser.bio || '' %></textarea></div>
                    <div class="text-right"><button type="submit" class="bg-sky-600 hover:bg-sky-700 text-white font-bold py-2 px-6 rounded-lg">Save Changes</button></div>
                </form>
                
                <form action="/profile/experience" method="POST" class="space-y-6 pt-8 mt-8 border-t">
                    <h3 class="text-xl font-semibold text-slate-700">Add Past Experience</h3>
                    <div class="grid grid-cols-1 md:grid-cols-2 gap-6">
                        <div><label class="block text-sm font-medium text-gray-700">Company Name</label><input type="text" name="company_name" required class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">Job Title</label><input type="text" name="job_title" required class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">Start Date</label><input type="date" name="start_date" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                        <div><label class="block text-sm font-medium text-gray-700">End Date (leave blank if current)</label><input type="date" name="end_date" class="mt-1 block w-full border-gray-300 rounded-md shadow-sm p-2 border"></div>
                    </div>
                    <div class="text-right"><button type="submit" class="bg-green-600 hover:bg-green-700 text-white font-bold py-2 px-6 rounded-lg">Add Experience</button></div>
                </form>
                </div>

            <div class="p-8 grid grid-cols-1 md:grid-cols-3 gap-8">
                <div class="md:col-span-2">
                    <h3 class="text-xl font-semibold text-slate-700 mb-4 border-b pb-2">About <%= profileUser.name.split(' ')[0] %></h3>
                    <p class="text-slate-600 leading-relaxed"><%= profileUser.bio || 'No bio provided.' %></p>
                    
                    <div class="mt-8">
                        <h3 class="text-xl font-semibold text-slate-700 mb-4 border-b pb-2">Work Experience</h3>
                        <% if(profileUser.past_companies && profileUser.past_companies.length > 0) { %>
                            <div class="space-y-6">
                                <% profileUser.past_companies.slice().reverse().forEach(exp => { %>
                                    <div class="flex gap-4">
                                        <div class="flex-shrink-0 w-12 h-12 bg-slate-200 rounded-full flex items-center justify-center"><i class="fas fa-briefcase text-slate-500 text-xl"></i></div>
                                        <div class="flex-grow">
                                            <div class="flex justify-between items-start">
                                                <div>
                                                    <p class="font-bold text-lg text-slate-800"><%= exp.job_title %></p>
                                                    <p class="text-md text-slate-600"><%= exp.company_name %></p>
                                                    <p class="text-sm text-slate-500">
                                                        <%= exp.start_date ? new Date(exp.start_date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) : '' %> - 
                                                        <%= exp.end_date ? new Date(exp.end_date).toLocaleDateString('en-US', { month: 'short', year: 'numeric' }) : 'Present' %>
                                                    </p>
                                                </div>
                                                <% if(user && user._id.toString() === profileUser._id.toString()) { %>
                                                    <form action="/profile/experience/delete/<%= exp._id %>" method="POST" onsubmit="return confirm('Are you sure you want to delete this experience?');">
                                                        <button type="submit" class="text-red-500 hover:text-red-700 text-xs p-2"><i class="fas fa-trash"></i></button>
                                                    </form>
                                                <% } %>
                                            </div>
                                        </div>
                                    </div>
                                <% }) %>
                            </div>
                        <% } else { %>
                            <p class="text-slate-500">No work experience added yet.</p>
                        <% } %>
                    </div>
                    </div>

                <div class="space-y-4">
                    <h3 class="text-xl font-semibold text-slate-700 mb-4 border-b pb-2">Contact & Details</h3>
                    <p><strong class="text-gray-500 w-20 inline-block">Email:</strong> <span class="text-slate-800 break-words"><%= profileUser.email %></span></p>
                    <p><strong class="text-gray-500 w-20 inline-block">Contact:</strong> <span class="text-slate-800"><%= profileUser.contact || 'Not provided' %></span></p>
                    <p><strong class="text-gray-500 w-20 inline-block">Batch:</strong> <span class="text-slate-800"><%= profileUser.batch || 'Not provided' %></span></p>
                    <p><strong class="text-gray-500 w-20 inline-block">Branch:</strong> <span class="text-slate-800"><%= profileUser.branch || 'Not provided' %></span></p>
                </div>
            </div>
        </div>
    </div>

    <script>
        function toggleEditForm() { document.getElementById('editForm').classList.toggle('open'); }
        function togglePictureForm() { document.getElementById('pictureForm').classList.toggle('open'); }
    </script>
</body>
</html>